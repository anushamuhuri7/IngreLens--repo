# Node / Frontend
node_modules/
dist/
package-lock.json

# Environment
.env
.env.local
.env.*.local
# frontend/.env is committed so Vite can read REACT_APP_ vars at build time
!frontend/.env

# Python / Backend
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.db
*.sqlite3
runtime.txt

# Supabase local
supabase/.temp/

# OS
.DS_Store
Thumbs.db

.vercel
memory/secrets.local.md
.env.*
*.env
credentials.json
*.pem
*.key
.credentials

# frontend/.env is committed so Vite can read REACT_APP_ vars at build time — must
# come LAST so it overrides every generic .env / *.env / .env.* pattern above.
!frontend/.env
